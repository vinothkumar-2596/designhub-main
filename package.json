{
    "name": "designhub-monorepo",
    "private": true,
    "workspaces": [
        "client",
        "backend"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=client & npm run dev --workspace=backend",
        "build": "npm run build --workspace=client",
        "install:all": "npm install"
    },
    "devDependencies": {
        "sass-embedded": "^1.97.3"
    },
    "dependencies": {
        "react-glass-ui": "^1.2.2",
        "react-icons": "^5.5.0",
        "recharts": "^3.7.0"
    }
}
